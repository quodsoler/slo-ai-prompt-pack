---
import BaseLayout from '../layouts/BaseLayout.astro';
import { legalNotice } from '../data/legal-content';
---

<BaseLayout
  title="Aviso Legal | Pack de 275+ Prompts IA"
  description="Aviso legal del Pack de 275+ Prompts IA para Marketing y Negocios. Información identificativa conforme al artículo 10 de la LSSI."
  canonicalPath="/aviso-legal"
>
  <article class="py-12 md:py-16 px-4">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-3xl font-bold mb-10">{legalNotice.title}</h1>

      {legalNotice.sections.map((section) => (
        <div class="mb-8">
          <h2 class="text-xl font-semibold text-text-primary mb-4">{section.heading}</h2>
          <div class="text-text-secondary leading-relaxed whitespace-pre-line">{section.content}</div>
        </div>
      ))}
    </div>
  </article>
</BaseLayout>
