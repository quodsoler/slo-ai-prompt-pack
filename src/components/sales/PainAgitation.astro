---
import { painPoints } from '../../data/sales-copy';
---

<section id="pain-agitation" class="py-16 md:py-24 px-4">
  <div class="max-w-4xl mx-auto">
    <h2 class="text-2xl md:text-3xl font-bold text-center mb-12">
      ¿Esto te suena, verdad?
    </h2>

    <div class="space-y-8">
      {painPoints.map((point) => (
        <div class="border-l-4 border-primary/60 pl-6 py-2">
          <p class="font-semibold text-lg text-text-primary mb-2">{point.lead}</p>
          <p class="text-text-secondary leading-relaxed">{point.text}</p>
        </div>
      ))}
    </div>
  </div>
</section>
