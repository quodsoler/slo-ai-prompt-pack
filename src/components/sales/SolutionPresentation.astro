---
import Badge from '../ui/Badge.astro';
import { differentiators } from '../../data/sales-copy';
---

<section id="solution" class="py-16 md:py-24 px-4 bg-surface/50">
  <div class="max-w-4xl mx-auto">
    <div class="text-center mb-12">
      <Badge variant="blue">La Solución</Badge>
      <h2 class="text-2xl md:text-3xl font-bold mt-4">
        Presentamos el Pack de 275+ Prompts IA para Marketing y Negocios
      </h2>
      <p class="text-text-secondary mt-4 max-w-2xl mx-auto leading-relaxed">
        No es otro curso de IA. Es una herramienta práctica con 275+ instrucciones probadas, listas para copiar y pegar en ChatGPT o Claude y obtener resultados profesionales en segundos.
      </p>
    </div>

    <div class="space-y-6">
      {differentiators.map((diff) => (
        <div class="flex gap-4 items-start">
          <div class="flex-shrink-0 w-6 h-6 mt-1 rounded-full bg-success/20 flex items-center justify-center">
            <svg class="w-4 h-4 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
          </div>
          <div>
            <p class="font-semibold text-text-primary">{diff.title}</p>
            <p class="text-text-secondary mt-1">{diff.text}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
